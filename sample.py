def average_even (numbers):
  total=0
  count=0

  for i in range(len(numbers)):
    if numbers[i]%2==0;
      total += number[i]
      count +=1
  return total/count

nums=[1,3,5,7]
print(average_even(nums))
